package util

import "testing"

func TestRepack(t *testing.T) {
	// tmpDir := t.TempDir()
	// err := CopyToWorkDir("test")
	//
	//	if err != nil {
	//		t.Error(err)
	//	}
	//
	// t.Error(GCWorkDirs(tmpDir))
	// t.Fail()
}

// TODO: finish writing test?
// looks like this is where it was left off
